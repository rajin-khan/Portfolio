# Required for Vercel serverless functions with pnpm
# pnpm creates symlinks in node_modules which Vercel can't resolve
# This hoists all dependencies to the root so they're actual files, not symlinks
shamefully-hoist=true
public-hoist-pattern[]=*
